<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>org.janelia</groupId>
	<artifactId>flythrough</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<parent>
		<groupId>org.scijava</groupId>
		<artifactId>pom-scijava</artifactId>
		<version>38.0.1</version>
	</parent>

	<name>Flythrough</name>
	<description>GUI-driven fly-through movie authoring for large 3D microscopy volumes (N5 / OME-Zarr).</description>

	<organization>
		<name>Howard Hughes Medical Institute, Janelia Research Campus</name>
		<url>https://www.janelia.org</url>
	</organization>

	<licenses>
		<license>
			<name>BSD-3-Clause</name>
			<url>https://opensource.org/licenses/BSD-3-Clause</url>
			<distribution>repo</distribution>
		</license>
	</licenses>

	<properties>
		<enforcer.skip>true</enforcer.skip>
		<package-name>org.janelia.flythrough</package-name>
		<main-class>org.janelia.flythrough.Flythrough</main-class>
		<license.licenseName>bsd_3</license.licenseName>
		<license.copyrightOwners>Howard Hughes Medical Institute</license.copyrightOwners>

		<!-- kept in sync with hot-knife: BDV 10.6.5 + imglib2 8 + N5 4 (Zarr v3) -->
		<bigdataviewer-core.version>10.6.5</bigdataviewer-core.version>
		<bigdataviewer-vistools.version>1.0.0-beta-36</bigdataviewer-vistools.version>
		<imglib2.version>8.0.0</imglib2.version>
		<imglib2-cache.version>1.0.0-beta-20</imglib2-cache.version>
		<n5.version>4.0.1</n5.version>
		<n5-zarr.version>2.0.1</n5-zarr.version>
		<n5-imglib2.version>8.0.0</n5-imglib2.version>
		<n5-universe.version>3.0.1</n5-universe.version>
		<n5-blosc.version>2.0.0</n5-blosc.version>
		<n5-zstandard.version>2.0.0</n5-zstandard.version>
		<!-- n5-universe 3.0.1 requires the AWS SDK v2 based releases -->
		<n5-aws-s3.version>5.0.0</n5-aws-s3.version>
		<n5-google-cloud.version>6.0.0</n5-google-cloud.version>
	</properties>

	<dependencies>
		<!-- N5 / Zarr access + OME-NGFF metadata -->
		<dependency>
			<groupId>org.janelia.saalfeldlab</groupId>
			<artifactId>n5-universe</artifactId>
			<version>${n5-universe.version}</version>
		</dependency>
		<dependency>
			<groupId>org.janelia.saalfeldlab</groupId>
			<artifactId>n5</artifactId>
			<version>${n5.version}</version>
		</dependency>
		<dependency>
			<groupId>org.janelia.saalfeldlab</groupId>
			<artifactId>n5-imglib2</artifactId>
			<version>${n5-imglib2.version}</version>
		</dependency>
		<!-- N5Factory references the S3 / GCS backends even for local files -->
		<dependency>
			<groupId>org.janelia.saalfeldlab</groupId>
			<artifactId>n5-aws-s3</artifactId>
			<version>${n5-aws-s3.version}</version>
		</dependency>
		<dependency>
			<groupId>org.janelia.saalfeldlab</groupId>
			<artifactId>n5-google-cloud</artifactId>
			<version>${n5-google-cloud.version}</version>
		</dependency>
		<dependency>
			<groupId>org.janelia.saalfeldlab</groupId>
			<artifactId>n5-zarr</artifactId>
			<version>${n5-zarr.version}</version>
		</dependency>
		<dependency>
			<groupId>org.janelia</groupId>
			<artifactId>n5-zstandard</artifactId>
			<version>${n5-zstandard.version}</version>
		</dependency>
		<dependency>
			<groupId>org.janelia.saalfeldlab</groupId>
			<artifactId>n5-blosc</artifactId>
			<version>${n5-blosc.version}</version>
		</dependency>

		<!-- imglib2 core + caching + transforms + ImageJ bridge -->
		<dependency>
			<groupId>net.imglib2</groupId>
			<artifactId>imglib2</artifactId>
		</dependency>
		<dependency>
			<groupId>net.imglib2</groupId>
			<artifactId>imglib2-cache</artifactId>
		</dependency>
		<dependency>
			<groupId>net.imglib2</groupId>
			<artifactId>imglib2-realtransform</artifactId>
			<version>4.0.5</version>
		</dependency>
		<dependency>
			<groupId>net.imglib2</groupId>
			<artifactId>imglib2-ij</artifactId>
		</dependency>

		<!-- BigDataViewer: interactive viewer + offscreen renderer + overlays -->
		<dependency>
			<groupId>sc.fiji</groupId>
			<artifactId>bigdataviewer-core</artifactId>
		</dependency>
		<dependency>
			<groupId>sc.fiji</groupId>
			<artifactId>bigdataviewer-vistools</artifactId>
		</dependency>

		<!-- ImageJ (CLAHE via mpicbg.ij.clahe.Flat) + mpicbg (BlockStatistics for CLLCN) -->
		<dependency>
			<groupId>net.imagej</groupId>
			<artifactId>ij</artifactId>
		</dependency>
		<dependency>
			<groupId>mpicbg</groupId>
			<artifactId>mpicbg</artifactId>
		</dependency>
		<dependency>
			<groupId>mpicbg</groupId>
			<artifactId>mpicbg_</artifactId>
		</dependency>

		<!-- config serialization + CLI -->
		<dependency>
			<groupId>com.google.code.gson</groupId>
			<artifactId>gson</artifactId>
		</dependency>
		<dependency>
			<groupId>info.picocli</groupId>
			<artifactId>picocli</artifactId>
		</dependency>
	</dependencies>

	<repositories>
		<repository>
			<id>saalfeld-lab-maven-repo</id>
			<url>https://saalfeldlab.github.io/maven</url>
		</repository>
		<repository>
			<id>scijava.public</id>
			<url>https://maven.scijava.org/content/groups/public</url>
		</repository>
	</repositories>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>11</source>
					<target>11</target>
				</configuration>
			</plugin>

			<!-- Uber jar with a runnable Main-Class -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<configuration>
					<transformers combine.children="append">
						<transformer
							implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
							<mainClass>${main-class}</mainClass>
						</transformer>
						<!-- keep N5 compression/codec service registrations so Zarr v2/v3
						     (gzip, blosc, zstandard) decode from the shaded jar -->
						<transformer
							implementation="org.apache.maven.plugins.shade.resource.AppendingTransformer">
							<resource>META-INF/json/org.janelia.saalfeldlab.n5.Compression$CompressionType</resource>
						</transformer>
						<transformer
							implementation="org.apache.maven.plugins.shade.resource.AppendingTransformer">
							<resource>META-INF/json/org.janelia.saalfeldlab.n5.serialization.NameConfig$Name</resource>
						</transformer>
						<transformer
							implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
					</transformers>
					<filters>
						<filter>
							<artifact>*:*</artifact>
							<excludes>
								<exclude>LICENSE</exclude>
								<exclude>LICENSE.txt</exclude>
								<exclude>META-INF/DEPENDENCIES</exclude>
								<exclude>META-INF/LICENSE</exclude>
								<exclude>META-INF/NOTICE</exclude>
								<exclude>META-INF/*.MF</exclude>
								<exclude>META-INF/*.SF</exclude>
								<exclude>META-INF/*.DSA</exclude>
								<exclude>META-INF/*.RSA</exclude>
								<exclude>module-info.class</exclude>
								<exclude>META-INF/versions/9/module-info.class</exclude>
							</excludes>
						</filter>
						<filter>
							<!-- artifacts exist in n5-universe -->
							<artifact>*:n5-imglib2</artifact>
							<excludes>
								<exclude>canonical.json</exclude>
								<exclude>cosem.json</exclude>
								<exclude>n5-compression.jq</exclude>
								<exclude>n5.jq</exclude>
								<exclude>zyx.zattrs.json</exclude>
							</excludes>
						</filter>
					</filters>
				</configuration>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
